export default function PreferencesContent() {
    return (
      <div>
        <h2 className="text-2xl font-semibold mb-4">Preferences</h2>
        {/* Add fields as per preferences */}
        <p>This is preferences content. You can add form fields here.</p>
      </div>
    );
  }
  